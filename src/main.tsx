import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';         // 若用 Tailwind

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
